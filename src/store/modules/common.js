const state = {
  headerTitle: ''
}
const getters = {}
const actions = {}
const mutations = {
  // 设置header标题
  setHeaderTitle: (state, data) => {
    state.headerTitle = data
  }
}
export default {
  state,
  getters,
  actions,
  mutations
}
